% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_gas_flux.R
\name{model_gas_flux}
\alias{model_gas_flux}
\title{Model gas flux from Gasmet data}
\usage{
model_gas_flux(
  data,
  ppm_var,
  seconds_var,
  co2_mod = NULL,
  min_n = 6,
  min_R2 = ifelse(gas_name == "CO2", 0.98, 0.1),
  ...,
  gas_name = NULL,
  force = FALSE
)
}
\arguments{
\item{data}{A data frame containing the time series of gas concentrations}

\item{ppm_var}{<\code{\link[rlang:args_data_masking]{data-masked}}> Variable in
\code{data} containing gas concentration values in parts per million or mg/L.}

\item{seconds_var}{<\code{\link[rlang:args_data_masking]{data-masked}}> Variable in
\code{data} containing time values in seconds (must start at 0)}

\item{co2_mod}{CO2 flux model object of class \code{flux_mod.CO2} (output of this
function). Required when modeling gas fluxes that use CO2-determined
deadband for data filtering.}

\item{min_n}{Minimum number of observations required in model. Default: 6}

\item{min_R2}{Minimum R-squared value required for a model to be considered
successful. Default: 0.98 for CO2, 0.1 for other gases.}

\item{...}{Additional arguments passed to gas-specific modeling methods. For
CO2 models, can include \code{deadband_opts}. For N2O models, can include
\code{mod_opts} list with \code{models} and \code{selection_metric} specifications. See
\code{Details}.}

\item{gas_name}{Gas name: "CO2", "N2O", "CH4", or "NH3". If NULL (default),
attempts to deduce from \code{ppm_var} column name (following Gasmet naming
conventions).}

\item{force}{Logical. If \code{TRUE}, returns model results even when quality
criteria (min_n, min_R2) are not met. Currently does not force data through
modeling when deadband removal is deployed and the number of observations
is < min_n, Default: FALSE}
}
\value{
An object of class \code{flux_mod} (and \verb{flux_mod.<gas_name>}) containing:
\item{gas_name}{Gas species name}
\item{ppm_raw, seconds_raw}{Original input data}
\item{ppm_processed, seconds_processed}{Data after deadband removal}
\item{min_n, min_R2}{Quality criteria used}
\item{success}{Logical indicating if modeling was successful}
\item{reason}{Character string explaining failure if not successful}
\item{models}{List of fitted model objects (linear, quadratic)}
\item{selected_model}{Name of the selected best model}
\item{metrics}{Model performance metrics (R2, RMSE, AIC, BIC, etc.)}
\item{deadband_info}{List with deadband removal method details}
\item{force}{Whether force option was used}
}
\description{
Model gas flux from Gasmet measurements. This function models
raw gas concentrations over time and supports multiple deadband
identification methods for CO2, CO2-determined deadband removal for all
gases, and model selection (linear/quadratic) for N2O. Returns a
comprehensive flux model object of class \code{flux_mod} and
\verb{flux_mod.<gas_name>}.
}
\details{
\subsection{Workflow}{
\enumerate{
\item \strong{Data validation}: Checks that seconds start at 0 and sufficient
non-NA/non-zero values exist
\item \strong{Deadband removal} (CO2 only): Applies specified method to remove
initial chamber warm up
\item \strong{Model fitting}:
\itemize{
\item CO2: Linear model after deadband removal
\item N2O: Linear and quadratic models using CO2-determined deadband, selects
best by RMSE or R2
\item Other: Linear model using CO2-determined deadband (if \code{co2_mod} is
specified) or using all data (if \code{co2_mod} is \code{NULL})
}
\item \strong{Quality assessment}: Checks against min_n and min_R2 criteria
}
}

\subsection{Deadband options for CO2 models Pass a \code{deadband_opts} list via \code{...}}{

with structure:

\if{html}{\out{<div class="sourceCode">}}\preformatted{deadband_opts = list(
  method = c("fixed", "minima", "optimum", "none"),
  fixed = list(seconds = 30),           # Remove first N seconds
  minima = list(portion = 0.25,         # Remove up to first 25\% of data
                seconds = NULL),        # OR remove data before N seconds
  optimum = list(trim_tails = FALSE) # Iteratively remove points to maximize R2
)
}\if{html}{\out{</div>}}
}

\subsection{Model selection options for N2O Pass a \code{mod_opts} list via \code{...} with}{

structure:

\if{html}{\out{<div class="sourceCode">}}\preformatted{mod_opts = list(
  models = c("linear", "quadratic"),  # Models to fit
  selection_metric = c("RMSE", "R2")  # Metric for best model selection
)
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Basic CO2 modeling
co2_result <- model_gas_flux(
  data = gasmet_data,
  ppm_var = Carbon.dioxide.CO2,
  seconds_var = seconds,
  gas_name = "CO2"
)

# CO2 with custom deadband
co2_result <- model_gas_flux(
  data = gasmet_data,
  ppm_var = Carbon.dioxide.CO2,
  seconds_var = seconds,
  gas_name = "CO2",
  deadband_opts = list(
    method = "fixed",
    fixed = list(seconds = 45)
  )
)

# N2O modeling using CO2 model for deadband
n2o_result <- model_gas_flux(
  data = gasmet_data,
  ppm_var = Nitrous.oxide.N2O,
  seconds_var = seconds,
  co2_mod = co2_result,  # from previous CO2 modeling
  gas_name = "N2O",
  mod_opts = list(
    models = c("linear", "quadratic"),
    selection_metric = "RMSE"
  )
)
}

}
\seealso{
\itemize{
\item \code{\link[=calculate_gas_flux]{calculate_gas_flux()}} for converting model slopes to flux rates
\item \code{\link[=import_gasmet_data]{import_gasmet_data()}} for importing raw Gasmet data
\item \code{\link[=import_chamber_volume]{import_chamber_volume()}} for chamber dimension data
}
}
